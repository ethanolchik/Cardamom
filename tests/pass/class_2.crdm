import io;

public class Counter(private start: int) {
    public static species: string = "human";
    private static hidden: string = "secret";

    public reveal() -> string {
        return Counter::hidden;
    }
}

fn main() {
    io.println(Counter::species);

    let c: Counter = new Counter(1);
    io.println(c.reveal());
}
