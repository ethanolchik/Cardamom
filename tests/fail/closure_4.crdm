fn main() {
    let increment: fn(&mut int) -> void = |x| void -> { x += 1; };

    // Declared type and closure signature disagree.
    let closure_ret_closure: fn() -> fn(&mut string) -> void = || fn(&mut int) -> void -> { return increment; };

    let n: int = 1;
    closure_ret_closure()(n);
}
